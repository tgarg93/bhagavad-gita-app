# Dharma — Product Spec

Behavior contracts for shipped features: what the app is *supposed* to do, where the code alone wouldn't tell you. **Maintenance rule: this file updates in the same commit as the feature it describes.** Companion docs: `docs/release-checklist.md` (test cases), `docs/dharma-illustration-spec.md` (art), repo-root `CLAUDE.md` (conventions & invariants).

Last synced: July 2026 (commit series through the onboarding family-stream change).

---

## 1. Onboarding

Eleven steps in one state machine (`OnboardingScreen`), Krishna asking each question via `KrishnaGuide`. Renders **outside the navigator** (App.tsx swaps it for AppNavigator until `profile.onboarded`).

**Steps are named, not numbered.** `step` indexes into an ordered `StepId[]`; every branch tests `id === 'name'` rather than `step === 2`. It used to be a bare index hardcoded in seven places with no shared source of truth, so inserting a screen meant renumbering by hand. Adding a screen is now one line in one array.

**Two intros come first, and they are skipped on replay.** `mode='edit'` (passed by the Profile tab's "Edit my answers") filters `introApp` and `introKrishna` out of the sequence — a returning user changing their daily goal should not be re-introduced to the app or to Krishna.

| Step (`StepId`) | Content | Writes |
|---|---|---|
| `introApp` | **Dharma says what it is for**, revealed line by line (lotus → three questions → body → the promise last and alone). Lotus, then three questions the app actually answers — *Why do we light lamps at Diwali? Why is Ganesha greeted first? What does karma actually mean?* — then *"You have probably been asked. You may have guessed. **Dharma is where you stop guessing — and become the one who knows.**"* **Krishna does not appear on this screen** (`KrishnaGuide` is gated off it): the app speaks first. | — |
| `introKrishna` | **Krishna introduces himself.** *"Namaste. I am Krishna. When Arjuna lost his way, I guided him. That conversation became the Gita. I will do the same for you — at every step, and whenever you ask."* Names the Gita as credential without retelling it. | — |
| `name` | "So — what may I call you?" (skippable: "I'd rather not say") | `name` (first name only; skip preserves any earlier name) |
| `familiarity` | Familiarity (new / some / deep) | `familiarity` |
| `intentions` | **"What would you like to be able to do?"** (multi-select, 6 options) — *Answer my kids' questions · Explain Hinduism to others · Understand our rituals · Finally read the Gita · Know the gods and stories · Steady myself in hard times*. **Outcomes, not shelves**: the old options ("Learn the Bhagavad Gita", "Understand Hindu philosophy", "Stories & festivals", "Personal growth") were the app's own table of contents read back to the user. | `intentions` |
| `goal` | Daily goal (5/10/15/20 min) — CTA "I'm committed" | `dailyGoalMinutes` |
| `identity` | **Jigyasu identity card**: 🪷 "YOU BEGIN AS Jigyasu — The Curious · Level 1 of 7" + `LEVEL_MEANINGS[1]` + the rung strip (outlined current rung, empty). CTA "See your journey →" — the card **morphs** (rise/shrink/fade, skipped under reduce-motion) into… | — |
| `journey` | **Your Spiritual Journey** finale: path view (rail + stage cards), entered with the lead Jigyasu card settling in (`entrance` prop) → Continue | — |
| `chai` | Daily rhythm: Krishna introduces Daily Chai; live preview of today's unified chai card → Continue | — |
| `sendoff` | **Auto-advances** (~2.5s) into the first lesson ("Getting started with '{next step}'…" + spinner) via `setPendingStart()` + finish. Only escape: "I'll explore on my own" text link → Home | `onboarded: true` |

- **Krishna types.** Every one of his onboarding lines reveals a character at a time (`KrishnaGuide`'s `typewriter` prop, ~22ms/char). Tapping the bubble completes it instantly and **Continue is never blocked** — nothing is locked behind an animation. Reduce-motion renders the full text at once. The full string is rendered invisibly underneath to reserve the bubble's final height, so it doesn't jump on every line wrap.
- **He asks, then the answers arrive.** A step's content — the name field, the option chips, the Jigyasu card, the journey path, the chai preview — waits for Krishna's line to finish typing, then rises in (chips staggered ~70ms apart). Driven by `KrishnaGuide`'s `onTypingDone` → a `krishnaDone` flag that resets on every step change and **starts true on the steps where he says nothing** (`introApp`, which animates itself, and `sendoff`) — otherwise their content would wait on a callback that never fires. The journey path is *mounted* late rather than faded in, because `JourneyPathView`'s own `entrance` stagger runs on its mount and would otherwise be spent behind the typing.
- **One avatar size throughout.** The avatar is fixed-width next to a `flex: 1` bubble, so a larger one on the intro squeezed the text column to ~200pt. `KrishnaGuide`'s `typewriter` is **off by default**: the callers that re-render on focus would otherwise re-type on every visit. (The Profile tab no longer opens with a `KrishnaGuide` greeting — the same identity now leads the level card itself.)
- **Animations are keyed on content, not on mount.** OnboardingScreen re-renders on every keystroke of the name field, so the app intro is its own component (mounts once) and the typewriter keys its effect on the message string. Get this wrong and both replay mid-name.
- `finish()` **merges** the patch (never overwrites the rolling summary/knowledge). Completing onboarding is the warm moment for the notification-permission ask.
- **Begin handoff**: onboarding can't navigate (outside navigator) → `journeyService.setPendingStart()`; HomeScreen's focus effect consumes the flag and opens the first unfinished item. Skip lands on Home.
- **Every option label fits on one line.** The label column is 290pt (390 screen − 48 body padding − 48 option padding − 4 border) at 18pt/600 ⇒ **~28 characters**; subtitles (14pt) get ~40. Copy that busts the budget wraps and the list stops scanning. This is a copy constraint, not a layout one — shorten the words, don't shrink the type.
- **Krishna's context block** (`krishnaContextService.buildContextBlock()`) is the primary consumer of onboarding answers — pasted into his Gemini prompt on every chat, reflection and follow-up. The **daily discovery pick** (`dailyPickService`) is the second: it reads `interests` + `familyStream` to rank which unseen content each day's Daily Chai (and its notification) surfaces. **Everything else still ignores the profile**: not the journey order, not the Learn tab, and notification *timing* is identical for all users (a "5 min/day" and a "20 min/day" user get the same schedule — only the chosen content differs). So **no question may imply a payoff beyond Krishna and the discovery pick without wiring one first.** Waiting to be wired, if a future question wants to earn its keep: `STAGE_2_DEITIES` (`journeyPath.ts`) is a flat id list a deity answer could reorder, and the notification hours are hardcoded next to an existing `NotificationSettings` object.
- **The family-deity question is cut** (was: "Growing up, whose face was closest in your home?"). It asked for a fact many users don't have, in language they had to decode. The **`familyStream` field stays** on `SpiritualProfile` and Krishna still reads it — `finish()` merges, so users who answered it keep their answer; only the question is gone.
- `interests` remains in the profile schema but is no longer asked — filled opportunistically from chat + editable in the profile card.
- **Onboarding option strings are stored raw** and `userKnowledgeSchema.ts` keeps its own copy of them for the Profile tab's editor. **Change one, change both** or the editor offers a different set of answers than onboarding did.

## 2. Guided journey

**The journey is a curriculum, not a table of contents.** It used to contain everything the app had (82 items). It now contains only what a learner must walk to reach the next stage — **65 items** — and everything cut remains fully browsable in the Learn tab. *The journey curates; browse shows all.*

Six stages (`JOURNEY_MODULES`, `ALL_MODULES`), built by `buildJourneyPath()`. Each states **what you can do when you finish it** (`MODULE_OBJECTIVES`), and each **ends with a capstone whose rubric IS that objective** — so the promise is tested, not merely asserted. (`MODULE_OBJECTIVES` governs the capstone rubric; it is no longer printed on the stage card, which now carries only the level identity, module name, and one-line rationale.)

| Stage | Contents | Ends with | Confers |
|---|---|---|---|
| **0 · Foundations** | 8 parts (§ 2.1) | `foundations:capstone` | **Shishya** |
| **1 · The Core Ideas** | dharma, karma, samsara, moksha, brahman-atman, maya, three-gunas, ahimsa | `capstone:ideas` | **Sadhaka** |
| **2 · The Gods** | krishna, rama, shiva, ganesha, hanuman, durga, parvati | `capstone:gods` | **Bhakta** |
| **3 · The Gita** | all 18 chapters | `capstone:gita` | **Jnani** |
| **4 · The Stories** | 7 Ramayana kandas + nachiketa, svetaketu-salt, two-birds, maitreyi | `capstone:stories` | **Rishi** |
| **5 · Living It** | 3 yoga paths + diwali, holi, navratri, janmashtami, ganesh-chaturthi | `capstone:living` | **Guru** |

**Objectives** — 0: *Explain what Hinduism is to a friend.* · 1: *Explain karma, dharma and moksha properly — and correct someone who thinks karma means fate.* · 2: *Walk into a temple and know who you're looking at, and how they're related.* · 3: *Read the Gita end to end, and say what Krishna actually tells Arjuna to do.* · 4: *Tell the Ramayana. Know why Nachiketa questioned Death.* · 5: *Keep the festival year, know what puja is, and know which of the four paths fits how you're built.*

**Cut to self-serve** (all reachable from Learn): `hinduism-overview` and `branches-of-hinduism` (Foundations Parts 1–2 now cover exactly this ground), `prana`, `guru`, `bhakti-paths`; `brahma`, `lakshmi`, `saraswati`; the 6 raw Upanishad texts; 4 Upanishad dialogues; the 12 kathas; 4 minor festivals. Prayers were never on the path.

> **Before cutting anything from the path, check it is reachable from `contentAggregator.getContentSections()`.** `getMajorDeities()` used to filter to `category === 'major'`, which silently hid Parvati, Lakshmi and Saraswati from the Learn tab — they were reachable *only* via the journey, so trimming the journey would have orphaned them with no entry point anywhere in the app. It is now `getAllDeities()`.

### 2.1 Foundations — the Jigyasu track (Module 0)

- **What it is**: the first thing a new user walks. Seven parts of bite-sized cards plus a capstone, ~35–40 min, resumable — each part is its own journey item. Content lives in `src/data/foundations.ts`.

| # | Journey id (**permanent**) | Title |
|---|---|---|
| 1 | `foundations:name` | What Hinduism Is |
| 2 | `foundations:thread` | What Makes Someone Hindu |
| 3 | `foundations:claim` | Core Beliefs |
| 4 | `foundations:wheel` | Karma & Rebirth |
| 5 | `foundations:faces` | The Gods |
| 6 | `foundations:library` | The Scriptures |
| 7 | `foundations:living` | Rituals & Festivals |
| 8 | `foundations:capstone` | Explain It Yourself |

  The **ids no longer track the titles** — the titles were renamed for clarity (users couldn't tell what "The Thread" or "The Faces" would teach), and the ids can't be, because completion is keyed on them. Don't "fix" the slugs. The user-facing word is **Part** ("Foundations · Part 3 of 8"); the internal type is still `FoundationsAct`.
- **Why it exists**: user testing said the content was overwhelming and progress was invisible. `hinduism-overview` alone was 12 sections / 1,224 words / 15 swipes, and completing journey items awarded **zero** points, so walking Module 1 as designed moved no needle at all.
- **One idea per page.** A card is a `NarrativeSection` carrying a `takeaway` (the sentence the reader banks — it must stand alone, because part celebrations replay it), a short body (a few short paragraphs split on blank lines), an optional `bullets` list (rendered by `FoundationCard`; used where the body enumerates), an optional Sanskrit block, an optional figure, and an optional `deeper` ref into the existing long-form content. Body prose follows the **teaching-voice** standard (§8). **Nothing was deleted**: the eight core concepts are Stage 1 (read whole), and the five cut ones are self-serve — all of them are `deeper` targets from the cards.
- **The depth rework (July 2026 — Acts 3–7; the capstone act keeps its original shape; Acts 1–2 keep their one-card-per-idea shape but were later revised in the opening clarity pass below).** User testing: one card per big idea moved too fast. A reworked act teaches each concept as a **block** — banked card → key-word **term page** → supporting analogy/story card(s) → MCQ → **checkpoint** — and every concept carries a figure. Analogy threads run ACROSS acts by design: the ocean/drop (Act 3) returns as moksha's rivers-reaching-the-sea (Act 4, Mundaka 3.2.8); Act 3's lamps return as the Trimurti's faces (Act 5). All content is **American-spelled** (July 2026 sweep — practice/honor/color/gray/worshiped…; `gitaVerses.json` translations excluded as quoted public-domain text; all swaps are homophones, so recorded clips stay valid). The new page species are `NarrativeSection.kind` variants rendered by `ContentReaderScreen` ahead of the takeaway branch:
  - `kind: 'intro'` (`IntroCard`) — the on-ramp after the cover, built on the **same design component as the celebration recap** (Krishna's ringed avatar tucked onto a white card's shoulder) so the act opens and closes on the identical surface: a few small framing sentences, then the **"You'll learn"** checklist from `FoundationsAct.learnItems` (the single source that the waypoints tick and the celebration replays checked).
  - `kind: 'term'` (`TermCard`) — the flashcard page, visually its own species (warm-ivory panel, saffron eyebrow, Devanagari set huge): `keyVerse` = the word, `storyText` = the hold-onto-this framing, `bullets` = optional word-by-word rows, `reappears` = the forward pointer.
  - `kind: 'waypoint'` (`WaypointCard`) — the checkpoint after each concept, on the **same celebration-card surface as the intro and completion screen** (`GuidePanel`: ringed avatar tucked onto a white card): Krishna's one-line bridge + the learn list with `learnIndex` items ticked and the next one emphasized (no NEXT pill — the highlight carries it). One component, three moments: the intro promises the list, waypoints tick it, the celebration replays it done. **Waypoints are quiet**: no playback bar, no narration segments, no clip ever; during read-along the voice parks on them exactly like a check page, and the swipe into the next section resumes it.
  - **Supporting cards** set `banked: false`: they render as normal cards but never bank a point or enter recaps — `isBankedCard` (narrativeTypes) is the single gate for the bank call and every recap builder.
  - **Practice checks** (`McqCheck.practice`) are new MCQs with identical UX and zero points; `foundationsService.getStats()` excludes them via `PRACTICE_CHECK_IDS`. **The scored set is frozen** — see §7.
  - **Inline Sanskrit verses**: the verse block renders where the prose introduces it, not at the card's foot — authoring convention: pre-verse prose in `storyText`, post-verse prose in `teachingText` (`FoundationCard` renders storyText → bullets → keyVerse → teachingText, and narration speaks the same order).
  - A reworked act's **celebration** replays `learnItems` fully checked ("What you've just learned") instead of the takeaway recap; the header labels the new pages "Overview" / "Key word" / "Checkpoint" and counts only takeaway-bearing sections as cards ("Card 3 of 14"); the cover counts cards, not sections. Analogies are load-bearing content decisions: maya runs on the **highway mirage** (mṛgatṛṣṇā — the rope/snake tested confusing), Brahman/atman/tat-tvam-asi ride one **ocean/drop/water** thread into the salt story.
- **The opening clarity pass (July 2026 — Parts 1–2).** User feedback: Part 1 ("What Hinduism Is") led with what Hinduism is *not*, and Part 2 ("What Makes Someone Hindu") called membership "practice" without saying what the practice is or where the boundary lies. Both now **lead with a concrete, positive answer**. Part 1 opens on `f-name-what-it-is` (Hinduism as a *family of related traditions* sharing gods, a library, practice, and core ideas), then previews the belief spine in `f-name-core-preview` (Brahman · atman · karma · samsara · dharma · moksha) with an explicit "we unpack each one in the parts ahead" signpost; the old "let's start with what it is *not*" card (`f-name-no-founder`) is recast as a surprising fact *about* that family. Part 2 adds `f-thread-what-practice` (puja, mantra, festivals, samskaras, pilgrimage, dharma) and `f-thread-belonging` (Hindu identity as a born-into **belonging**, closer to a people than a creed — so a borrowed practice, e.g. a Muslim lighting a Diwali lamp, does not confer it; converts exist but there is no single gateway). The self-serve `hinduism-overview` concept got the same positive-first opening. New cards are `banked: false` and new checks `practice: true`, so the **87-point sum is unchanged** (§7). The capstone six-point rubric is now more directly satisfiable (point 1 "a family of traditions", point 6 "practice, puja, festivals"). Cost: four new clips + one re-record (`f-name-no-founder`), so a **native build**.
- **Read-along narration.** A Foundations act can ship **pre-recorded** narration — bundled ElevenLabs clips, one MP3 per section id in `assets/audio/foundations/`, switched on by presence in `foundationsAudioManifest.ts`. **All eight acts are live** (Parts 3–7 re-recorded July 2026 after the depth rework — 74 clips, scripts derived verbatim from `foundations.ts` by the transformer conventions in `docs/foundations-narration-script.md`, māyā pronunciation fixed track-wide). Waypoints deliberately have no clips (quiet checkpoints; the voice parks). Term/intro pages play clips without sentence highlight (their components aren't wired to segment ids). Beware the skip trap when editing content: prerecorded mode silently *skips* clip-less sections, so stale manifest entries must never stay live with rewritten text. ▶ plays one clip per section and the text highlights **sentence-by-sentence off real playback position** (`PrerecordedController`), in reading order: takeaway → story → each bullet. `audioNarrationService.buildFoundationsSegments` emits a block per bullet (`section-{i}-bullet-{j}`, matching `FoundationCard`) so bullets highlight in turn and the clip's time stays apportioned — otherwise the bullet audio would stretch the story highlight. Narration text is **verbatim with the on-screen card** so the highlight lines up; Sanskrit is spelled phonetically and `<break>` tags pace it. Adding/changing clips is a **native build, not OTA**; regenerate with `scripts/generate-foundations-audio.mjs` from `docs/foundations-narration-script.md`.
- **Figures enlarge on tap.** A card's optional figure is an SVG keyed by section id in `FoundationFigure.tsx`, drawn on a wide (~640-unit) canvas that shrinks to ~half size at phone width. Inline it stays a small **preview** with a "tap to enlarge" affordance; tapping opens the **same** SVG full-screen and **rotated to landscape** (the only way a ~3:1 figure fills a portrait screen), cloned large by the shared `Figure` wrapper so the vectors stay crisp. The Judaism/Christianity/Islam comparison is a real text table, not an SVG, so it is not tap-to-enlarge.
- **The test**: at the end a friend asks "so what actually is Hinduism?" and the reader answers in their own words. Krishna (Gemini) marks it against a six-point rubric, generously; **4 of 6 passes**.
- **Part celebrations**: each part ends on the standard `JourneyCelebration`, which additionally replays that part's banked takeaways ("You can now say…") and shows the `handoff` — the question the *next* part answers — directly above the next-step button. The celebration is wrapped in a `ScrollView` (flexGrow, not flex) because that extra content overflows a fixed page; short celebrations still center exactly as before.
- **Existing users are not rewound.** Foundations was inserted at the head of the path, so `foundationsService.init()` (called from HomeScreen before `getNextUnfinished`) silently marks all eight parts complete for anyone with ≥3 prior completions. The stage stays visible and re-openable; no rite is granted, so they can still take the capstone.

### 2.2 Knowledge checks (app-wide capability)

- `src/data/checkTypes.ts`. Any content whose sections are `NarrativeSection[]` can attach `checks` to a section; they render as their own pages **immediately after** it. Foundations is the first consumer — Gita chapters, deities and festivals can adopt them with no schema change.
- Three kinds. **`mcq`**: tap, instant feedback, and the `why` shows for a *wrong* answer too — the check teaches, it does not gate, and **a wrong answer still advances** (nothing is locked). **`recall`**: free text, graded by `checkService.gradeRecall`. **`reflect`**: points at an index in the content's existing `reflectionQuestions[]`, so it renders through the existing reflection page and `ChapterReflection` — one code path, one persistence path, and it still scores via the reflections × 15 term.
- **MCQs are Krishna's voice (July 2026).** The `mcq` page renders as a conversation, not a quiz card: the `prompt` is a Krishna `Bubble` (his question) and, after the tap, the `why` is his reply Bubble with a "Yes, that's it" / "Not quite" lead-in (`CheckPage.Mcq`, matching the sibling `recall` conversation). **Presentation only** — grading, `recordCheck`, scoring and the green/red correct-answer signal on the options are unchanged; no `why` strings were rewritten (the warm second-person prose already reads as Krishna).
- **Grading never happens in the model.** `checkService` asks Gemini only which rubric points are *present*; the verdict is `hit.length >= passCount`, computed in code. If the grader is unreachable it returns `null` and the UI reveals the model answer and lets the reader self-mark — the capstone must never be the one screen that blocks a level-up.
- **Read-along pauses on a check, never skips it.** Narration turns pages by audio segment and checks aren't segments, so the voice used to roll from one section straight into the next and scroll past the check between them. Now, when a section that ends into a check finishes, narration **pauses and lands on the check page** (`ContentReaderScreen` `onSegmentEnd`); swiping forward into the next section **resumes** it (`skipPage`). The reader answers, then continues — the check is never silently bypassed.

### 2.3 End-of-chapter Krishna dialogue (app-wide capability)

- **What it is**: a guided, Socratic conversation that closes a concept — Krishna asks the "big question," the reader taps a stance, Krishna responds, walking them to an insight. Data + types in `src/data/krishnaDialogues.ts`; rendered by `KrishnaDialoguePage`. It rides as a `dialogue` reader page **after the reflections, before the celebration** (`ContentReaderScreen`), lit up by data presence (`ReaderContent.krishnaDialogue`, attached by content ref in `readerContent.ts` — no new `ReaderContentType`, so the festival fallthrough is untouched).
- **Branching node graph**: a dialogue is `{ start, nodes }`; each node speaks (`krishna[]`, `**bold**` for key terms via `RichText`) then offers `choices` (each with its own `reply` + `next`, or a shared node-level `reply` when every choice converges), OR a free-text `input`. Supports re-explain branches ("say that again?") and a **free-text ending** where the reader names their murti — persisted to the profile (`DialogueInput.saveTo: 'familyStream'` → `updateSpiritualProfile`, so `krishnaContextService` carries it into later chats). A terminal node may carry a **`practice`** ("Try this week") — one concrete thing to start doing.
- **Authored, no scoring, nothing locked**: every path is valid (no "wrong" answers), all responses are written into the data — **zero Gemini calls, works offline**. It never scores and never blocks completion: a persistent **Skip** (and swipe) jumps straight to the celebration, and reaching the celebration still marks the item done. An **"Ask Krishna more ›"** footer opens the live Ask-Krishna sheet (the only path that spends chat quota, and only if the reader chooses). Citations render as self-explanatory footnotes after Krishna's reply (never a bare "(2.22)" in his speech).
- **Piloted on the five Core-Ideas concepts** (Module 1): `concept:samsara` → `concept:karma` → `concept:maya` → `concept:brahman-atman` → `concept:moksha` — one teaching arc, verse-anchored (Gita 2.22 / 2.47 / 7.14 / 3.20, Mundaka 3.2.8, Chandogya 6.13). GitaVersePlayer is not yet wired (no Gita-chapter dialogue in this set). **Future**: personalize each dialogue from `krishnaContextService` so examples speak to the reader's own life.


- **The celebration is not a reading position.** Reaching it marks completion, but the index is never persisted — saving it meant re-opening a finished item restored you onto "COMPLETED" with the reading behind you and no way back into it. Readers also clamp on restore, so positions already poisoned by that bug heal on the next open.
- **A completed item re-opens at its cover** (re-reading starts at the top). An unfinished one still resumes exactly where it was left.
- **The celebration offers "Read it again"** (pages back to the cover — it's the same pager, so no navigation) **and "Done"** (`goBack()`, returning you to the journey path you came from). "Done" used to be "Back to Learn", which jumped to the Scriptures tab and popped the path off the stack — that, plus the position bug, made completed content genuinely unreachable.
- **Completion = reaching an item's celebration page** (viewability callback fires `journeyService.markCompleted(id)`). First completion wins; id-keyed, permanent.
- **Next = first unfinished in path order** (wraps). Home's Continue card, notifications, and celebrations all derive from it.
- **Celebration page** (last page of every reader): entrance choreography (ring settle → checkmark spring → staggered text) + one-shot marigold shower (16 petals, ~2.5s, respects reduce-motion). Fires when the page scrolls into view, **once per arrival**. Next button turns the page to the next journey item; chapter→chapter stays inline in the Gita player.
- **Progress strip on the celebration** (`ProgressRungs`, shared with onboarding): current rung outlined + inner fill at `progressToNext`, completed rungs small solid, ahead rungs small track-tinted; only current + next rungs are large and named. Readers snapshot `getProgression().points` on mount so the fill animates from where the session started; line reads "+N points · M to {next}" (plain "points"; "+0" omitted; at Guru: "The path's last name is yours").
- **Level-up ceremony**: when `getProgression().level > level_last_celebrated` (AsyncStorage, default 1) at celebration time, a full takeover overlay rises after the entrance (~1.9s): gold identity card — old name struck through, new name + english + `LEVEL_MEANINGS[n]` + rung strip — its own marigold shower, and "Continue as {name} →" to dismiss. The level is persisted immediately so it never re-fires. Note: users who out-leveled their last ceremony (incl. pre-feature users) get one catch-up ceremony on their next completion — intended.
- **Path screen** (`JourneyPath` route) + onboarding finale share `JourneyPathView`: one continuous vertical **rail** (teal behind the walker, pale saffron ahead — the rail itself carries "where you are"; no marker pill). The **spiritual title is the card**: each of the six **accordion stage cards** leads with its level identity in "Name — The English" form as the dominant heading (Jigyasu, Shishya/Sadhaka/Bhakta/Jnani, Rishi — teal once the level is actually attained, else saffron), with the module name demoted to a small uppercase label beneath, then the one-line italic rationale (emoji glyph; no stage eyebrow, no "after this you can", no per-stage counts or rings; the ONLY numeric tracker is the screen subtitle "n of m steps walked"). A single enlarged rail **dot** sits beside each card's heading (Guru remains a standalone end-cap marker at the tail, no card to fold into). Cards expand to the item checklists: 40px cover tiles (done = faded + teal tick; current = saffron frame + saffron bold title, no pill; Gita tiles get chapter-number overlays). Current stage is saffron-framed and auto-expands. Titles are earned by engagement, so placement is soft by design. Nothing is locked.
- Home Continue card: body resumes next item; separate full-width footer band "View full path · n of m ›" opens the path screen (mistap-safe two-band design — keep bands separated).

## 3. Daily Chai (the daily loop)

- Home IS the brief: under the status row sits **ONE unified Daily Chai card** (`DailyChaiCard`, shared with the onboarding preview); reading requires zero taps. Opening Home marks the day's chai read (`daily_chai_last_opened`) and credits activity.
- **Rotation** (`buildDailyAtom(date, snapshot?)`, deterministic per date; `getDailyAtom(date)` is the snapshot-less sync fallback): weekday → type — Sun **verse**, Mon why, Tue saying, Wed **compare**, Thu story, Fri **question**, Sat word. Within a type the pick rotates **weekly** (`floor(localDayNumber/7) % pool` — the old dayOfYear hash froze each weekday on one atom all year). 109 authored atoms: 60 why (7 ritual-etiquette originals + the 53-question "always wondered" canon — iconography, the gods, nature, practice, beliefs, interleaved so consecutive Mondays vary), 18 compare, 10 story (4 link into the new story library via `story:` refs), 7 each saying/word/question.
- **Targeted discovery** (`discovery` type, `dailyPickService`): with a user snapshot, **~half the non-verse days** become a personalized pick — one specific piece of content the reader **hasn't seen yet**. The copy is **authored, not generated**: each card's **headline carries the insight on its own** (you get the "aha" without reading the body) and connects it to modern life, with the subject in the eyebrow ("MEET RAMA"), a body that grounds it in the story, and a real citation. Teasers live in **`src/data/discoveryTeasers.ts`** keyed by content ref, and **discovery is gated to teaser-backed refs** (∩ unseen via `content_completion` ∩ routable) so a flat catalog blurb can never ship; a discovery day with no eligible teaser falls back to the authored rotation. Voice follows §8 (Teaching voice + Reads-human texture, calibrated to `f-wheel-karma`): plain, no em-dashes, no "X, not Y" slogans. The pool is **blend-ranked** (interest/`familyStream` overlap + proximity to the journey's next step + level fit) then **interleaved across kinds** (deity → concept → story → festival → practice → scripture) so consecutive discovery days span the catalog. `buildDailyAtom` walks the ranked pool by ordinal (`floor(localDayNumber/2) % pool`), so day-to-day picks differ. Snapshot is read **once** (async) and selection stays **synchronous**, so HomeScreen (today) and the 28 pre-baked notifications agree per day. The **50/50 split of non-verse days is a one-line knob** (`isDiscoveryDay`).
- **The chai is Home's hero**: `DailyChaiCard` is a **breathing card** — the insight is a large pull-quote (~24px), with `spacing.lg` padding and airy vertical rhythm, given extra separation above `chaiSlot` so it anchors the top of Home rather than reading as one card among the status row and Continue card.
- **Across traditions (`compare`, Wednesdays)**: Hinduism set beside other faiths (Trimurti vs Trinity, karma vs sin, Om/Amen/Amin, reincarnation vs resurrection…). Framing rules: curiosity never superiority; Hindu locus cited precisely; other faiths characterized respectfully at intro level; differences stated without ranking. Card renders with the hook/body layout and an indigo accent.
- **Sensitive questions** (caste, women & scripture, swastika, vegetarianism) are included deliberately — they are the most-asked questions about Hinduism — and answered with the citation discipline: what scripture says vs. what is later social history, stated honestly. Customs without scripture (right hand, 108, shoes off) say so on the card.
- **Verse days** synthesize the atom from `getDailyVerse` (deterministic from 701 bundled verses): English-first hierarchy, Devanagari beneath. The standalone verse card is gone.
- **Festival accent** (`getFestivalLensFor`, date-relative): on a festival's **eve or principal day only**, the slot becomes a generated atom from festival data. Deliberately bounded to those two days — the old "within 7 days of the next festival" window let a multi-day festival (Ratha Yatra, `duration: 9`) own the brief for ~16 straight days, because the countdown clamped to 0 for its whole run and it out-sorted everything else. Keying strictly off each occurrence's start caps it at 2 days per festival, and computing against the passed `date` (not "today") means the 28 pre-baked notifications each get their own day's festival, not today's stamped 28×.
- **Per-type treatments** (one shared frame; per-type inner layout + accent border/tag tint): word = large centered Devanagari + transliteration + gloss (saffron); saying = quote glyph + Devanagari + "WHAT IT MEANS" interpretation (teal); question = airy centered italic question + short insight (violet); verse = quote + Sanskrit line (gold); story/why/festival/**discovery** = hook/body (saffron; discovery is teal, with a `tagOverride` eyebrow). Structured Sanskrit lives in the optional `atom.sanskrit {devanagari, transliteration, meaning?}` field.
- **Voice mode**: ▶ on every card (toggle — tap again stops). Sanskrit-bearing cards speak the Devanagari first in the Hindi voice (slower/lower, same settings as reader narration), then the English; **verse cards stay English-first**. No Hindi voice installed → Sanskrit part is skipped silently (`speakSequence` in `audioNarrationService`; a cancellation token stops mid-sequence chains).
- Card actions are **bare icons** in the header row (no chips/circles); card bodies are not tappable. No chat-bubble action — today's `krishnaPrompt` (present on all types, incl. synthesized verse atoms) leads Ask Krishna's suggestions instead.
- **The citation IS the source link — there is no 📖 button.** The old book icon opened `atom.link`, a hand-authored *further-reading* pointer written independently of the citation and rendered right beside it, so a card citing the Isha Upanishad opened **Karma**. `AtomLink` is deleted. The citation now carries the only link, via `atom.sourceRef` — the permanent content-ref form with an optional fragment naming the exact spot (`gita:2#47`, `scripture:isha-upanishad#isha-first-verse`), resolved by the same `routeForContentRef` / *"Read in app ›"* affordance the reader's own citation footnotes use.
- **A citation links only when the app actually holds the cited text.** 45 of 112 atoms carry a `sourceRef` (33 Gita loci → the exact verse; Katha/Isha/Mundaka/Mandukya/Taittiriya Upanishads and the Ramayana kandas → the exact section). The rest cite **Puranas, the Rig Veda, Brihadaranyaka, Chandogya, the Mahabharata — none of which have a reader** — and keep a plain, unlinked citation. **Never substitute a loosely-related concept for a missing source**; that substitution is precisely the bug this replaced.
- **Deep links land on the thing, not the cover**, and **outrank a saved reading position** — following a citation into an item you were part-way through must open the cited section, not where you left off (`ContentReaderScreen`'s `sectionId` param; `GitaVersePlayerScreen`'s `verse` param).
- `DailyChaiCard` takes its source handler as a **prop** (`onOpenSource`) and must never call `useNavigation()` — `OnboardingScreen` renders the card **outside the navigator**, so a hook in there crashes onboarding. Onboarding passes no handler, so its citation stays plain.

## 4. Notifications (local today; remote push specced in § 4.1 for wave 2)

All current notifications are **local** — scheduled on-device, no backend, no APNs. Idempotent reschedule-all on every app open. "Fires only after absence" = one-shots scheduled for future days, cancelled+rescheduled on each open.

| Type | When | Content | Deep link |
|---|---|---|---|
| Daily Chai | 8:00 daily (28 one-shots) | "☕ Your chai is ready" + that day's atom hook | the content the pick names (`{url:'contentref', ref}` → reader/Gita/detail), or Home tab when the atom has no `sourceRef` |
| Journey nudge | 19:00 on days 1/3/7/14/21/28 of absence | next step title | JourneyPath |
| Festival | 9:00 three days before + 8:30 day-of (next 4 festivals) | name/countdown/significance | FestivalDetail |
| Streak protection | 21:00 tomorrow if streak ≥ 1 | "n-day streak on the line" | JourneyPath |

Tap handling: `navigationRef` + response listener in App.tsx (incl. cold-start via `getLastNotificationResponseAsync`). Permission is requested only at warm moments (onboarding finish, celebrations) and never re-nagged.

### 4.1 Remote push (future — wave 2, not implemented)

**Why**: local one-shots exhaust after the last app open — the lapsed user, the one notifications exist for, eventually hears nothing. Remote push is the only complete fix. The cheap interim **shipped July 2026** (build 9): 28 Daily Chai one-shots + absence nudges through day 28 + next 4 festivals ≈ 42 pending, comfortably under iOS's 64-pending limit. Decision recorded in the production plan: push stays out of v1.

**Requires**:
- Backend + device push-token registry — build alongside accounts in wave 2; until accounts land, tokens key to the local anonymous id.
- Delivery: start with the **Expo Push Service** (`getExpoPushTokenAsync`, server POSTs to Expo's push API — no APNs plumbing). Direct APNs/FCM is the later opt-out path if Expo's service becomes a constraint.
- Credentials: APNs key on the Apple team that owns `com.tushargarg.dharma` (managed via EAS credentials); FCM key for Android. The push entitlement arrives with the build config — nothing manual in Xcode.
- Server-side scheduler that reproduces atom content: the **authored/festival/verse** path (`getDailyAtom(date)`) is deterministic per date — needs a build step exporting atoms to JSON so local and remote say the same thing on the same day for free. The **discovery** path is snapshot-dependent (per-user unseen + profile), so the server would either mirror the user's completion/profile state or fall back to the authored path for remote pushes; simplest is remote-only-on-authored-days.

**Unlocks**: win-back after 7+ days of silence; festival-day pushes regardless of last open; announcements/content drops; server-side timezone correctness.

**Coexistence rules** (the contract future work must honor):
- Local notifications remain the primary channel — offline and no-account users keep working unchanged.
- No double-fire: the server only sends what the device cannot have pending — i.e., remote takes over after N days of silence, inferred from an app-open heartbeat, and never duplicates the locally scheduled week.
- Same deep-link payload shape (`{url, festivalId?, ref?}` — `url:'contentref'` carries a content `ref` routed via `navigateToContentRef`) so App.tsx tap handling works unchanged for both channels.
- Permission UX unchanged (warm-moment ask, never re-nagged); token registration happens only after grant. New AsyncStorage keys for token/consent — append-only, as always.

## 5. Readers

- **Anatomy** (both readers): horizontal paged FlatList — cover (art, title, meta, Begin) → one section per page → one reflection question per page (skippable) → celebration. Resume position stored per item.
- **Reflection questions**: one plain, concrete question per item (two only when the content genuinely has two distinct hooks) — answerable from ordinary daily life in a sentence, no stacked metaphors. The page build is count-agnostic.
- **Citations**: sections carry an optional one-line `citation` footnote (hairline rule, small italic) at the page foot; the full `SourcesCard` bibliography rides at the bottom of the **last text page**. No standalone sources page. Citations and sources whose subject exists in the app carry a content ref (`citationLink` / `appLink`, e.g. `gita:2`, `deity:krishna`, `festival:janmashtami-2025`) and render a tappable "Read in app ›" affordance — resolved by `routeForContentRef`/`navigateToContentRef` (journeyPath.ts), pushed as a fresh reader instance. Refs to texts not in the app stay plain text.
- **Section subtitles**: left-aligned under the title, one shared treatment across readers (bodyLG italic, soft ash) — matching the Gita chapter subtitle.
- **Narration**: expo-speech; Indian-English voice preferred, Hindi voice for Sanskrit (skipped if unavailable); plays under the iOS silent switch (session activated by a silent WAV once per session). Transport: play/pause, prev/next section, ±10s. Skip buttons move voice AND page together. **The playback bar appears only inside content** (verse/section and reflection pages — never covers or celebrations). **Begin on a cover turns the page AND starts narration automatically**; play pressed mid-content starts from the current page (start segment resolved by id, not arithmetic).
- **Background listening + Now Playing controls**: narration keeps playing when the app is backgrounded, the phone locks, or it's on car Bluetooth (`staysActiveInBackground`, requires the `UIBackgroundModes: audio` entitlement — build 13). While a metadata-bearing narration session is active, the app publishes a **Now Playing card** (`nowPlayingService` → the `expo-now-playing` local native module → `MPNowPlayingInfoCenter`) showing the **chapter/act title + cover art** on the lock screen, Control Center, the **CarPlay Now Playing screen**, and Bluetooth head units — **no CarPlay entitlement** (the system fills the CarPlay Now Playing tab for any audio app). Remote controls (`MPRemoteCommandCenter`) are **play/pause + skip ±15s** (no lock-screen scrubber); each routes back into the same transport the on-screen controls use (skip = ±15s within the clip for prerecorded, ±1 segment for TTS), and the on-screen play/pause icon stays in sync via `NarrationCallbacks.onPlayStateChanged`. The two screens that supply metadata are `ContentReaderScreen` (`content.title` / `readerLabel` / `coverImage`) and `GitaVersePlayerScreen` (chapter name + `getChapterCover`); sessions without metadata (the `AudioControls` bar) publish nothing and clear any stale card. **Progress on the bar is exact for prerecorded Foundations (real clip position, resets per section) and estimated for TTS** (WPM heuristic). Covers are bundled `require()` assets resolved to a URI via `Image.resolveAssetSource`. Audio interruptions (calls) are not auto-resumed. Adding/changing the native module is a **native build, not OTA**.
- **Ask Krishna (readers)**: a floating ringed Krishna avatar (52pt, the GuidePanel ring) pinned bottom-right above the playback bar in all three players (ContentReader, GitaVersePlayer, PrayerPlayer); it dims to ~60% while a page swipe is in motion. **Content pages only — never covers (it collides with Begin) or celebrations (Krishna is already on that card).** Section pages carry extra bottom scroll-padding so the last lines clear it. Tap opens the Ask Krishna **chat sheet** in place (§6) — the reader never loses their page. This replaced the readers' "Ask Krishna about this" ⋮ item and the prayer player's header chat icon; ContentReader hides the ⋮ entirely when the menu would be empty (no `detailRoute`). Context seeding is unchanged: section/verse pages self-seed (with snippet) on page change; opening the sheet from any other page seeds coarse `{type, title}` so the sheet can never open on stale context.
- **Interstitials (Jigyasu framing) are data-gated, not Foundations-gated**: the intro "what you'll learn" checklist, the "N of M banked" waypoints, the cover `kicker`, and the celebration recap all render for **any** reader content that supplies the data (`learnItems` + `kind:'intro'`/`'waypoint'` sections + `kicker`). The **`handoff`** ("what's next") teaser renders above the next-step button on the celebration. The **content-upgrade program** (bring every track to the Jigyasu bar — bite-sized cards, interleaved checks, and these interstitials; sequenced by journey module, Gita last) is extending the full model beyond Foundations, **piloted on `concept:karma`** (Module 1): 9 banked cards, 2 key-word (`term`) pages, 2 waypoints, 4 MCQ checks, an intro learn-list of 6, and a handoff to samsara. Non-Foundations checks/cards score normally (additive-safe; Foundations' 87-point sum is untouched). **Wave 1 (all 8 Core-Ideas concepts) is complete.** **Wave 2 (The Gods)** applies the same model to deities, with one rule: a deity's `stories` stay a **narrative coda** after the teaching sections (auto-appended by `storyToSection`) — the `learnItems`/waypoints/checks scope to the teachings, and the coda opens on a "three tales…" on-ramp section; story prose is authored to be vivid and scene-told. Piloted on `deity:krishna` (handoff → rama).

### 5.1 Prayer learn player (`PrayerPlayerScreen`)

The "skills" content shape: liturgical text learned by repetition, not read once. Data in `src/data/prayers.ts` (`prayer:<id>` ids are permanent — completion keys on them).

- **Anatomy**: paged like the readers — cover (attribution, verse/lesson count, "First lessons" badge while `complete: false`) → intro pages (origin story; last one carries when-to-recite) → **one verse per page** → celebration.
- **Verse page (recitation-first)**: hero text is the script being recited from — the **Aa/अ header toggle** swaps transliteration-first ↔ Devanagari-first; meaning sits below a hairline divider; a **mala-bead row** shows position (beads grouped by lesson, current bead enlarged).
- **Modes**: **Learn** (default) — stay on the verse, ▶ loops its audio ×1/×3/∞ (loop pill). **Listen** — the whole prayer flows, audio auto-advancing the pages to the end. Manual page turns stop a playing verse; the loop pill is disabled in Listen.
- **Audio**: existing TTS pipeline, Devanagari as `sanskrit` segments (Hindi voice, slow rate; silently skipped when no Hindi voice — same rule as everywhere).
- **Progress**: resume via the shared reader-positions map (key `prayer:<id>`); finishing a **complete** prayer writes `prayer:<id>` to `content_completion` and increments `prayer_recitations` (append-only key; times-recited shown on the celebration). Incomplete prototypes celebrate the lesson but persist nothing. Points formula untouched.
- **Editorial**: scriptural mantras cite their locus (Mahamrityunjaya: Rig Veda 7.59.12); compositions name author and century (Chalisa: Tulsidas, 16th c.; Om Jai Jagdish Hare: Shardha Ram Phillauri, 1870s); legends told as legends. Every "how to practice" intro ends with "This is one common way — ask your family how they do it."
- **Surfaces**: "Prayers & Mantras" section in Learn (category `mantras` → PrayerPlayer); `prayer:` refs resolve via `routeForContentRef`; the Krishna FAB seeds `type: 'prayer'` context (the header chat action was removed with the FAB's arrival).
- **v1 library (shipped)**: Hanuman Chalisa (flagship, 43 verses / 11 lessons of 4, `complete`), Om Jai Jagdish Hare, Sukhkarta Dukhharta (Ganesha), Aarti Kunj Bihari (Krishna), Mahamrityunjaya, Om Namah Shivaya, Shanti Mantras (Saha Navavatu + Purnamadah). All `complete: true`. The `complete` flag gates completion-writing, so a partially-authored prayer can ship and teach without marking done.

### 5.2 Story library (`src/data/stories.ts` → ContentReader)

Standalone kathas and Upanishad dialogues in the reader's `NarrativeSection[]` shape — no new screen. One schema, one `collection` tag (`'upanishad' | 'katha'`); `story:<id>` ids are permanent.

- **Adapter**: `readerContent.ts` gains a `story` branch; `detailRoute` is now **optional** and stories omit it, so the reader's ⋮ menu hides "Details & practices" (its only reader-code change). `readerLabel` is "Upanishad Story" / "Story".
- **Upanishad collection (8)** — Nachiketa & Death, Svetaketu & the salt (tat tvam asi), Satyakama Jabala, Yajnavalkya & Maitreyi, Gargi's debate, Indra & Virochana, the two birds, Bhrigu's five sheaths. **Appended to journey Module 2** after Gita 18 via `UPANISHAD_JOURNEY_ORDER` (append-only — existing item positions never move).
- **Katha collection (12)** — Vishwamitra & Vasishtha's cow, Harishchandra, Dhruva, Markandeya vs. death, Ekalavya, King Shibi & the dove, Sudama's rice, Gajendra's surrender, Prahlada, Savitri, Ganesha & the moon, the churning of the ocean. **Browse-only** (Learn tab), off the journey.
- **Surfaces**: two Learn sections ("Stories of the Upanishads", "Timeless Kathas", category `stories` → ContentReader); `story:` kind in `routeForContentRef`; all 10 Daily Chai story atoms carry a `sourceRef` "Read More ›" into the reader (via `story:`, or a deity-section fragment for the Ganesha/Krishna tales, or a scripture ref for Nachiketa & Hanuman).
- **Editorial**: per-section `citation` footnotes with `citationLink` refs where the subject is in-app (e.g. Prahlada → `festival:holi-2025`, Markandeya → `deity:shiva`); folk episodes (Ganesha & the moon) cited as tradition honestly; injustice left visible where the source leaves it (Ekalavya).
- **Story readability (2026-07-21, after a "wall of text" report on `nachiketa`)**: story `storyText` is authored as short paragraphs split on a blank line (`\n\n`), and body prose is **left-aligned, never justified** (justify forced the dense wall; the fix in `NarrativeSections.tsx` is app-wide). **Dialogue formatting (set-apart speech):** a paragraph beginning with the marker `»` renders as a lifted speech block (saffron left tick + small-caps teal speaker label + italic line); `» Speaker: line` carries a label, `» line` is unattributed. Everything else is narration prose. One shared parser, `parseStoryBeats` + `DIALOGUE_BLOCK_BASE` (`src/data/storyBeats.ts`), drives BOTH the renderer and `sectionsToNarrationContent`, so read-along highlight stays in lockstep — beat blocks occupy narration indices `DIALOGUE_BLOCK_BASE`+ (past the six fixed blocks), and a dialogue section must NOT also carry `teachingText`/`keyVerse` (those narrate out of visual order). Applied to the dialogue-driven Upanishad tales (`nachiketa`, `svetaketu-salt`, `indra-virochana`; the other five to follow). Plain multi-paragraph stories keep the legacy single-block render. `»` (U+00BB) is used because it never begins real prose and is apostrophe-safe in single-quoted TS strings.

### 5.3 Multi-part scriptures (`src/data/scriptureTexts.ts` → ContentReader)

Long texts read in the **Gita reading pattern** but via the shared reader (no full verse JSON like the Gita's). One schema serves both collections: a `ScriptureCollection` (Ramayana, Principal Upanishads) holds ordered `ScripturePart`s (kandas / individual Upanishads), each a reader item. `scripture:<partId>` ids are globally unique and permanent; parts are authored at Gita-chapter depth (`NarrativeSection`s with cited `openingVerse`/`keyVerse` shlokas, story + teaching voice, per-section `citation`, cross-linked to episode-stories via `citationLink` e.g. Katha → `story:nachiketa`).

- **Surfacing**: the existing "Ramayana" / "Principal Upanishads" scripture cards route (via `collectionForCardId`) to **`ScriptureContentsScreen`** — a contents list (collection blurb + ordered part rows with completion ticks) → each part opens `ContentReader`. Other scriptures still use `ScriptureDetail`; `bhagavad-gita` still opens the Gita player.
- **Reader adapter**: `readerContent.ts` gains a `scripture` branch (no `detailRoute`; `readerLabel` = collection title); `scripture:` kind in `routeForContentRef`; `ReflectionContentType` + Krishna `CurrentContent.type` gain `'scripture'` (append-only unions).
- **Journey**: Ramayana kandas append to Module 2, Upanishad texts to Module 1, via `RAMAYANA_JOURNEY_ORDER` / `UPANISHAD_JOURNEY_ORDER` (append-only — existing positions never move).
- **v1 scope**: Ramayana = 7 kandas (Bala…Uttara); Upanishads = Core 6 (Isha, Kena, Katha, Mundaka, Mandukya, Taittiriya). Phase A shipped Bala Kanda + Katha Upanishad; remaining parts land in follow-up passes. Per-part cover art is a follow-up (illustration spec); parts reuse existing covers for now.
- **Ramayana → narrative-reader rework (2026-07, all seven kandas)**: the kandas were condensed interpretive summaries; a review reframed them as a **faithful narrative retelling** — the actual story with **character dialogue kept in** (the `»` speech blocks, which render on the scripture path with no code change: `hasStoryBeats`→`NarrativeSections`), inline word glosses (`**putrakameshti (a fire-sacrifice for a son)**`), **sparse interpretation** in their own `sectionHeader:'What this means'`+`teachingText` sections (a handful per kanda, never one per scene), 1–2 `kind:'term'` key-word pages, and **2–3 MCQ `checks` spaced at chapter breaks** (attached to an arc's last section so they fire once, after the reading, not mid-flow). Constraints: a `»` dialogue section must NOT also carry `teachingText`/`keyVerse` (they narrate out of visual order); non-Foundations MCQs score normally (no `practice` flag; additive, the 87-point Foundations sum is untouched). Part-level `kicker` (cover) + `handoff` (into the next kanda's celebration) are populated. All seven kandas (Bala→Uttara) are reworked; sensitive passages (Vali's ambush, the agni pariksha, Sita's banishment) are kept honest — state what the text does, note it has been questioned for centuries, never tidy it into a moral. `TermCard` renders a term's `deeper` invitation as a tappable "Go deeper" row (dharma/maya/… → their concepts).
  - **Read-along narration (2026-07, pilot: Bala Kanda)**: scripture readers can ship the same **bundled ElevenLabs (Oliver) clips** as Foundations — one MP3 per section id in `assets/audio/ramayana/`, switched on by presence in `scriptureAudioManifest.ts` (a parallel of the Foundations manifest, resolved through the same generalized `prerecordedClipsFor` — `contentType === 'scripture'`). The scripture path reuses the TTS `audioSegments` (from `sectionsToNarrationContent`) so the `PrerecordedController` sentence-highlight tracks for free, verse sections included. Scripts are **transformed from the on-screen prose** (verbatim with the page): `**bold**` stripped, `»` dialogue flattened to spoken lines, `openingVerse` read as its **English meaning** (not chanted), and Sanskrit names respelled phonetically for a British reader (Ah-YOHD-yuh, Vaal-MEE-kee, Rah-MAH-yuh-nuh…). Generated by `scripts/generate-ramayana-audio.mjs` (CLI substring filter regenerates one kanda at a time — `node … ayodhya`). Bundled → **native build, not OTA**. **All seven kandas recorded** (Bala pilot + the other six, 68 clips; the recurring name dictionary — Rah-MAH-yuh-nuh, DHUR-muh, MAH-yaa, HUH-noo-mahn, RAH-vuh-nuh, LUNG-kuh, VAH-nuh-ruh… — is applied inside the `SCRIPTS` map).

### 5.4 Festival library (`src/data/festivals.ts` → ContentReader + FestivalDetail)

18 festivals, all at full reader depth (5–7 `NarrativeSection`s with citations + `SourcesCard`, plus detail-screen rituals/traditions/regional variations). `festival:<id>-2025` ids are permanent (the `-2025` suffix is vestigial; `occurrences` carries 2025–2028 per entry).

- **Coverage** (July 2026 expansion, 9 → 18). Original nine: Makar Sankranti, Basant Panchami, Maha Shivratri, Holi, Ram Navami, Janmashtami, Ganesh Chaturthi, Navratri, Diwali. Added nine — pan-Indian: Raksha Bandhan, Dussehra (standalone; Navratri keeps the Durga framing, Dussehra tells Rama/Ravana + Ramlila), Guru Purnima, Hanuman Jayanti, Karwa Chauth; regional anchors (`importance: 'regional'`): Onam, Chhath Puja, Ugadi & Gudi Padwa (one entry, two names), Ratha Yatra. Deliberately folded, not standalone: Pongal/Lohri/Magh Bihu (inside Makar Sankranti), Durga Puja (inside Navratri), Dhanteras/Govardhan/Bhai Dooj (inside Diwali). Deferred: Gita Jayanti, Teej, Akshaya Tritiya.
- **Dates**: every `occurrences` array is verified against Drik Panchang **pinned to New Delhi** (observance dates differ by a day between India and the US; the app uses India dates app-wide) — except Onam, pinned to Thiruvananthapuram. Multi-day entries anchor the **main day** (Diwali precedent): Onam anchors Thiruvonam (Atham begins 10 days earlier, told in the narrative), Chhath anchors Sandhya Arghya (`duration: 2` covers Usha Arghya), Ratha Yatra anchors departure (`duration: 9` to Bahuda).
- **Surfaces**: Learn tab and the calendar's list view use `getAllFestivals()` (all entries, sorted by next occurrence) — **never `getMajorFestivals()`**, which would silently hide the regional four. Month grid, Daily Chai countdown atoms, and festival notifications pick new entries up automatically via the existing date helpers.
- **Editorial**: same rules as everywhere (§8) — scripture-anchored stories cite loci (Vamana → Bhagavata Purana Canto 8; Ravana's fall → Yuddha Kanda 102–111); folk material labeled honestly (Karwa Chauth vrat katha, the Maveli song, sindoor/opened-chest stories); documented history cited as history (Tagore's 1905 rakhi movement, ISKCON 1968, Ramlila's UNESCO listing). Cross-links ride `citationLink` (Onam ↔ Holi via Prahlada/Bali; Dussehra ↔ Navratri; Raksha Bandhan's raksha mantra ↔ Onam's Bali).
- **Journey untouched**: `STAGE_5_FESTIVALS` stays the curated five; the other 13 are self-serve in Learn. New entries confer no points (not journey items) — progression invariants unaffected.
- Cover art: the new nine ship on the generic fallback; prompts are a follow-up in the illustration spec's Festivals table.

## 6. Ask Krishna

- Gemini 2.5 Flash; persona + compact context block: profile (name, familiarity, intentions, interests, family stream — "Not sure" excluded), progression level/stats, rolling profile summary, structured knowledge facts, and current content when arriving from a reader.
- Chat bubbles = the shared reflection `Bubble` (avatar outside: Krishna left, user photo right). Suggested prompts: today's chai `krishnaPrompt` first, then persona starters.
- Knowledge system: opportunistic extraction from conversation into structured fields + rolling ≤120-word summary; profile card shows completion and allows edits.
- **Entry points**: the tab, plus the reader FAB → a slide-up **chat sheet** (`KrishnaChatSheet`, 86% tall, grip + tap-outside + X + Android back to dismiss; deliberately no swipe-down — no gesture-sheet dependency in the app). The sheet header carries a context chip naming the current page (the reader's headerInfo, e.g. "Karma · Card 2 of 4"). Sheet and tab share one `geminiService` session and one screen-state core (`useKrishnaChat`) — a thread started in the sheet continues on the tab. The chat **re-seeds only when the content's identity changes** (coarse key: type/title, chapter for the Gita), never on page/snippet changes — so swiping pages mid-reading no longer restarts an ongoing thread. Sheet suggestions lead with one page-aware chip from the current content, then persona starters; chips fill the input, never auto-send.
- **Free-tier limit**: **5 user-sent chat messages per device-local calendar day**, one pool across sheet + tab. Enforced in `geminiService.sendMessage` via `chatQuotaService` (`krishna_chat_quota` AsyncStorage key — append-only, local-only, deliberately not synced); consumed on **successful** sends only (failures/429s/offline never burn one); reflections and check grading (`generateOneOff` paths) never count. UI: quiet "N of 5 questions left today" caption above the input on both surfaces; exhausted = disabled input + hidden chips + one derived Krishna-voice closing bubble (vanishes after midnight on its own). Client-side only — the server `ai_usage` RPC (10/min, 60/day) stays the hard backstop; server-side tightening is tracked future work.
- **Streaming**: chat replies stream token-by-token so words appear as Krishna "types" (the "reflecting…" indicator shows only until the first token). The chat path uses its own generation config (`CHAT_GENERATION_CONFIG`: `thinkingBudget: 0`, `maxOutputTokens: 640`) — thinking off removes the pre-reply reasoning delay, and the shorter cap keeps replies to 2-4 sentences; grading/reflection keep the fuller `GEMINI_CONFIG`. The proxy gained a `stream` branch (`:streamGenerateContent?alt=sse`, piped straight through) and a `warm` branch (returns before Gemini, no cost) that the surfaces ping on open to dodge cold starts. RN's fetch can't read a streaming body, so the client uses an `XMLHttpRequest` incremental read; it degrades to the full response at completion if progressive reads don't fire. Non-chat callers stay on the buffered `{text}` path.
- **Telemetry**: `ask_krishna_opened` `{source: fab|tab}`, `krishna_chat_limit_reached` — ids/counts only, per the telemetry rule.

## 7. Progression & profile

- **Levels**: Jigyasu(0) → Shishya(100) → Sadhaka(300) → Bhakta(700) → Jnani(1500) → Rishi(3000) → Guru(5000 pts).
- **Points = versesRead×2 + chaptersCompleted×30 + reflections×15 + cardsBanked×1 + checksPassed×2 + ritesPassed×30 + journeyItemsCompleted×30.** The first three terms are the original invariant and are untouched. The Foundations terms are **additive only** — a user with no Foundations activity has them all at zero and therefore *exactly* the point total they had before. (A check is worth **2**, retuned pre-launch from 4 so that 2–3 questions per part still fit under the Shishya gate — see below.) Never add a term that can subtract.
- **`journeyItemsCompleted` EXCLUDES Foundations and capstone items.** Foundations is already scored per card (87 points), and the 87-under-100 gap is what makes its capstone the thing that tips a reader into Shishya; paying another 8×30 there would destroy that gate. Capstones pay a rite instead. **Completing a journey item used to be worth zero** — which is why a user could finish every festival and every practice and move no needle at all.
- **A rite is a floor, never a ceiling.** `level = max(levelForPoints(points), highest conferred rite level)`. There are **six rites — one per stage.** This is the only reason Rishi and Guru are reachable: the old journey topped out at 2,962 points against thresholds of 3,000 and 5,000, so the top two levels could not be earned by any amount of reading. Guru remains above the point ceiling **by design** — it is conferred by the final capstone, not ground out. Implemented as a ceiling (`min(...)`) this would demote every existing user above 100 points and trap Gita readers at Jigyasu forever — hence the floor: a rite can only *raise* a level, so nobody is demoted and nothing is locked.
- **The gate is curriculum tuning, not enforcement.** The Foundations track sums to **87 points** (32 banked cards + 20 scored checks × 2 + 1 reflection) — deliberately short of the 100 for Shishya, so a reader who does every card and check but skips the capstone sits visibly short of it. Passing the capstone confers the level *and* adds 30 (→117), so it then holds on points alone. **Adding a reflection (×15), or any new scored points, levels the reader up mid-track and deflates the capstone.** The sum is **frozen at 87** by the depth-rework policy (July 2026): the 32 banked cards and 20 scored checks are the permanent scored set — intro/term/waypoint pages carry no takeaway, supporting cards set `banked: false`, and every check added since sets `practice: true` (0 pts, excluded via `PRACTICE_CHECK_IDS`). Follow the same pattern in every act rework and the sum never needs redoing (commented at the top of `foundations.ts`).
- **Reflections are deduped on a stable id** (`refl-gita-{ch}-{qi}` / `refl-{type}-{id}-{qi}`). `ChapterReflection` used to mint `refl-${Date.now()}-${random}` on every mount, so re-answering a question you had already answered appended a *second* entry — and since progression counts entries × 15, points were farmable indefinitely by reopening a page and typing again.
- `progressToNext` is clamped at **both** ends. A rite conferring level 2 at 87 points makes `points - level.minPoints` negative, which fills the gauge backwards without the lower clamp.
- **`LEVEL_MEANINGS`** (progressionService): one meaning paragraph per level — the single copy source for the onboarding Jigyasu card, every level-up ceremony, and (future) the Profile card. The path screen is titled **"Your Spiritual Journey"**.
- Home leads with the status row (🪷 level name → Profile tab). Profile tab: Partiful-style photo (also the tab icon), level card, "what Krishna knows" completion card, reminders toggles, dev tools (__DEV__). The **level card** shows the level name + points, then the same semicircular **arch gauge** the completion screen uses (`CelebrationGauge`, filled to `progressToNext` within the current band), the four stat tiles, and a **"How your level works"** row that opens a bottom sheet: the plain-language points table, the seven-step ladder (current rung lit), and the reader's next journey step.
- No streak display anywhere (product decision: noisy); streak feeds only the protection notification.

## 8. Editorial standard (all content)

- Four content shapes: **courses** (reader items on the journey), **atoms** (Daily Chai), **skills** (future: mantra learn-by-repetition), **discussions** (future: audio/voice-mentor renderings).
- Every item/section opens with the **human question it answers**, not a definition.
- **Bite-sized pages**: one idea per page — split into more sections rather than compressing; a course may span many pages like a Gita chapter. Stories are told as **scenes**, never summarized ("what actually happened," not "the story goes").
- **Readable formatting**: `**bold**` the key terms a reader should retain (rendered via RichText; markers stripped for narration/highlighting); use the section `bullets` field for any enumeration of 3+ items. Prose paragraphs stay short (2–4 sentences), separated by blank lines. *(Applied across all concepts, deities, and festivals — July 2026 rollout.)*
- **Teaching voice** *(Foundations — Act 1 is the reference, Acts 2–8 to follow)*: user testing found the prose clear but assuming — undefined terms and pronouns pointing back at the previous card. Every card must (1) **define before use** — no foreign/technical term without a plain gloss in the same breath; (2) **no cold pronouns** — never open a card with "it/this/they" pointing at the previous card; restate the subject; (3) **concrete before abstract** — a familiar example before the principle; (4) **one idea per sentence**; (5) **open with a hook back** — answer the question the last card raised; (6) **restate the key idea plainly at the end** — repetition is welcome. Cards run a little longer as a result; that is the trade.
- **Reads-human texture** *(July 2026 — the structural rules above were not enough; prose passing all six still read AI-generated, twice)*: (a) **em-dashes are near-banned in body prose** (storyText/teachingText/holds/bubbles/check whys) — use periods, commas, and plain connectives (so, and, because, but); the single dictionary-gloss dash survives ONLY in `keyVerse.meaning`, term meanings, and learn-item labels ("karma — action"); (b) **no aphorism stacks, chiasmus, or "X, not Y" epigram pairs** — a short fragment is fine when it reads as speech ("Nobody does."), not when it reads as a slogan; (c) **vary the endings** — at most one bolded sentence per card, and some cards end unbolded; (d) **write the sentence through** — no "Here is X:" / "Notice…" / "Watch…" scaffolding openers; (e) **elaborate rather than compress** — long plain sentences that take their time; (f) **simple spoken vocabulary**, glossing every term the moment it appears. **The calibration reference is the `f-wheel-karma` card** in `foundations.ts` ("Nobody does. There is no judge keeping score. There is only karma…") — new prose should sound like that card.
- **Orient-first & define-every-name** *(ENFORCED, 2026-07-21 — the `deity:krishna` pilot passed every rule above yet still assumed heavy prior knowledge and opened on opaque paradox lines; this is the corrective, and the rewritten `deity:krishna` is now the readability exemplar)*: (a) **orient before you dive in** — open each item by grounding a newcomer in *who/what it is and how it fits*, before any story or abstraction (a **deity**: which god, their role, and key relationships — e.g. Krishna is a human incarnation/*avatar* of Vishnu the preserver, hidden among cowherds from the tyrant Kamsa, who later speaks the Gita — drawn from the `familyConnections`/`significance`/`titles` fields; a **concept**: the plain idea in one sentence first); (b) **define every PROPER NOUN inline, not just technical terms** — no place, person, text, or Sanskrit word used without a plain gloss in the same breath: *Vrindavan (the cowherd village he grew up in)*, *Kamsa (the king warned this child would kill him)*, *gopis (the cowherd women)*, *avatar (a coming-down of God into a human life)*; (c) **no clever paradox / chiasmus lines, even as a "thesis"** — "the absolute is also adorable, and the adorable is also absolute" is exactly the trap; say the plain thing first, then (optionally) the resonant line; (d) **the test: a curious newcomer with zero background follows it on first read.** "Captivating" never overrides this — vivid, but never opaque. Applies to every content type, across the board. **(e) Reading-order, not eyeballed** *(added 2026-07-21 after a re-audit found the intro cards clean but narrative openings still cold — `nachiketa` opened "Vajashravasa performed the great sacrifice…", `karma`/`dharma` opened on "Arjuna … the battlefield" with the whole Gita frame assumed)*: gloss at the **first appearance in reading order** (cover → intro → each card → each appended tale) — a gloss in a *later* section does NOT rescue an earlier cold mention. Introduce the **central actor / setup** of a scene, not just the side figures (name Arjuna and the war, not only Yudhishthira). Give an assumed ritual/war/cosmology a plain clause ("a great sacrifice, a ceremony where…"). **Verify** with a grep-checklist of capitalized non-sentence-initial words + Sanskrit terms per item, then a newcomer re-read of the first two pages. (Watch single-quoted `storyText`: a straight apostrophe in a gloss breaks the parse — use plain phrasing or curly quotes.)
- **Citations**: famous, publicly verifiable loci only; public-domain translations named (Sivananda/Gita, Müller/Upanishads, Griffith/Rig Veda, Ganguli/Mahabharata); practices without scripture say "tradition" honestly; later-tradition stories labeled as such (e.g., Hanuman's opened chest).
- Ritual/practice pages end with the standing line: **"This is one common way — ask your family how they walk it."**
- Plurality is doctrine: no single "correct" Hinduism; branches taught explicitly. Comparative-religion content: last priority, expert-reviewed first, framed as shared questions before differing answers.

## 9. Known gaps (deliberate, tracked)

Built-but-dark (ship-or-delete pending): starter-pack commerce data, child-friendly Gita reader, Vapi voice path, inert AuthContext (plaintext passwords — delete before Wave 2 rebuilds auth). Absent by design until later waves: accounts/sync (all AsyncStorage), analytics, traditions vault. **Bundled ElevenLabs narration has shipped for Foundations Act 1** (see §2.1); the rest of the track is still system TTS. The Gemini API key ships in the bundle (acceptable for family beta; Wave 2 proxies it).
